import React from 'react';
import DynamicPage from '@/components/DynamicPage';

const Contact: React.FC = () => {
    return <DynamicPage slug="contact" title="Contact Us" />;
};

export default Contact;
