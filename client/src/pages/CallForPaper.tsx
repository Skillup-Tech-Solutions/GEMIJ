import React from 'react';
import DynamicPage from '@/components/DynamicPage';

const CallForPaper: React.FC = () => {
    return <DynamicPage slug="call_for_paper" title="Call for Papers" />;
};

export default CallForPaper;
