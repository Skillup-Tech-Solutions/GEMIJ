import React from 'react';
import DynamicPage from '@/components/DynamicPage';

const AimScope: React.FC = () => {
    return <DynamicPage slug="aim_scope" title="Aim & Scope" />;
};

export default AimScope;
