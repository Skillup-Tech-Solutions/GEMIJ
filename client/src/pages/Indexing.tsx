import React from 'react';
import DynamicPage from '@/components/DynamicPage';

const Indexing: React.FC = () => {
    return <DynamicPage slug="indexing" title="Indexing & Abstracting" />;
};

export default Indexing;
